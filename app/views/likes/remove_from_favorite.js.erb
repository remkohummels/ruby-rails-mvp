$('.item_vote').on('ajax:send', function() {
}).on('ajax:complete', function(data) {
  $(this).html("<%= j render @votable.class.to_s.tableize + '/add_to_favorite', item: @votable%>");
}).on('ajax:error', function() {
}).on('ajax:success', function(data) {
});
