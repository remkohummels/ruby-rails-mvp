<% @comment = locals[:comment] %>
<div class="comment">
  <label><%= link_to @comment.user.full_name, user_path(@comment.user) %> <%= "(Admin)" if @comment.user.admin? %></label>
  <li class="text"><%= @comment.message %></li>
</div>
